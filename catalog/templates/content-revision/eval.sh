#!/usr/bin/env bash
set -euo pipefail

# Configure a fixed rubric or evaluation set and a real scoring pipeline.
printf '%s\n' 'autoresearch: configure a content rubric evaluator that emits one finite numeric score' >&2
exit 1
