#!/usr/bin/env bash
set -euo pipefail

# Configure a repeatable benchmark and convert its measurement to one score.
printf '%s\n' 'autoresearch: configure a repeatable code benchmark that emits one finite numeric score' >&2
exit 1
