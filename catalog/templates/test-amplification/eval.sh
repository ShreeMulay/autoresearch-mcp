#!/usr/bin/env bash
set -euo pipefail

# A passing test command is a prerequisite, not a measurement of amplification.
# Configure mutation score, changed-line coverage, or another real signal.
printf '%s\n' 'autoresearch: configure a test amplification evaluator that emits one finite numeric score' >&2
exit 1
